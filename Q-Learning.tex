\documentclass[12pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}
\usepackage{mathptmx}
\usepackage[top=2.5cm, bottom=2.5cm, left=3.5cm, right=2.5cm]{geometry}

\usepackage{setspace}
\setstretch{1.5}

\usepackage{amsmath, amssymb}
\usepackage{amsfonts}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{graphicx}
\usepackage{subcaption}

\titleformat{\section}{\normalfont\fontsize{15}{18}\bfseries}{\thesection}{1em}{}
\titlespacing*{\section}{0pt}{*2}{*1}
\setlength{\parindent}{0pt}

\lstset{basicstyle=\ttfamily\small, breaklines=true, frame=single}

\begin{document}

\begin{titlepage}
    \centering
    \includegraphics[width=0.9\textwidth]{figure/Verona_Logo.png}\\[1.5cm]
    
    {\large Dipartimento di Informatica}\\[1cm]
    {\Huge\bfseries Q-Learning in ambienti a ricompensa sparsa: analisi sperimentale nell'ambiente DoorKey}\\[2cm]
    {\Large A.A. 2025/2026}\\[3cm]

    {\large Antongiulio Donno \quad VR502818}\\[0.4cm]
    
    \vfill
\end{titlepage}

\pagenumbering{roman}
\tableofcontents
\clearpage

\pagenumbering{arabic}
\setcounter{page}{1}

\section{REINFORCEMENT LEARNING}

\subsection{Introduzione al Reinforcement Learning}
Il Reinforcement Learning (RL) è un paradigma dell'apprendimento automatico in cui un agente intelligente impara a prendere decisioni ottimali interagendo con un ambiente, con l'obiettivo di massimizzare un segnale di ricompensa cumulativo nel tempo.
I concetti fondamentali del RL includono:
\begin{itemize}
    \item \textbf{Trial and error (Apprendimento per prove ed errori):} l'agente esplora l'ambiente in modo attivo, deducendo le strategie ottimali dalle conseguenze delle proprie azioni.
    \item \textbf{Delayed reward (Ricompensa ritardata):} le azioni intraprese possono avere conseguenze e generare ricompense solo a lungo termine.
\end{itemize}

L'obiettivo formale del RL è la stima accurata della \textbf{funzione di valore} \(V(s)\) e l'individuazione della \textbf{policy ottima} \(\pi^*(s)\), ovvero la mappatura stato-azione che massimizza il valore atteso della ricompensa futura.

\vspace{1em}
\noindent

Il problema è formalizzabile come un Processo Decisionale di Markov (MDP) in cui, a differenza della pianificazione classica:
\begin{itemize}
    \item La funzione di ricompensa \(R(s, a, s')\) è incognita.
    \item Le probabilità di transizione tra stati \(T(s, a, s')\) (la dinamica dell'ambiente) sono incognite.
\end{itemize}

Di conseguenza, l'agente deve acquisire campioni empirici provando azioni e raccogliendo le relative ricompense.

\subsubsection{Conoscenza del Modello}
Le strategie per risolvere problemi di RL si dividono in due categorie principali:
\begin{itemize}
    \item \textbf{Metodi Model-based:} tentano di stimare esplicitamente il modello dell'ambiente (le matrici di transizione e di ricompensa). Sfruttando queste stime, l'agente può pianificare le azioni riducendo l'interazione con stati a ricompensa negativa. Risultano più efficienti in termini di campionamento (sample efficiency), ma computazionalmente più complessi.
    \item \textbf{Metodi Model-free:} evitano di costruire un modello dell'ambiente, concentrandosi sull'apprendimento diretto della funzione valore-azione (Q-function) e della policy ottima tramite l'esperienza. Risultano più semplici da implementare e presentano un bias inferiore, sebbene richiedano una maggiore quantità di dati per convergere.
\end{itemize}

\subsection{Q-Learning}
Il Q-Learning è il principale algoritmo model-free per il controllo ottimo. Permette di stimare direttamente i valori di \(Q(s,a)\) approssimando l'equazione di ottimalità di Bellman senza necessitare della dinamica dell'ambiente:
\[Q_{k+1}(s,a) = \sum_{s'} T(s, a, s') \left( R(s, a, s') + \gamma \max_{a'} Q_k(s', a') \right)\]

\subsubsection{Sample-based Q-Learning}
Acquisendo una tupla di esperienza \((s, a, r, s')\), l'algoritmo aggiorna iterativamente la stima della funzione Q fondendo il valore storico con il nuovo target temporale (TD Target):
\[Q(s,a) \leftarrow Q(s,a) + \alpha \left( r + \gamma \max_{a'} Q(s', a') - Q(s,a) \right)\]

\vspace{1em}
\noindent
\textbf{Proprietà e Convergenza}
Il Q-Learning garantisce la convergenza asintotica alla reale funzione \(Q^*(s,a)\) (e quindi alla policy ottima) sotto due condizioni fondamentali:

\begin{itemize}
    \item \textbf{Esplorazione adeguata:} Ogni coppia stato-azione \((s,a)\) deve essere visitata un numero infinitamente grande di volte.
    \item \textbf{Decadimento di \(\alpha\):} Il learning rate deve diminuire progressivamente (es. \(\alpha = \frac{1}{n(s,a)}\)).
\end{itemize}

Il Q-Learning è intrinsecamente un algoritmo \textbf{Off-policy}: separa la policy usata per generare il comportamento (es. esplorazione epsilon-greedy) dalla policy che viene valutata e ottimizzata (puramente greedy, rappresentata dall'operatore \(\max_{a'}\)).

\pagebreak

\vspace{1em}
\noindent
Lo pseudocodice dell'algoritmo Q-Learning è il seguente:

\begin{lstlisting}
Initialize Q(s, a) arbitrarily for all s in S, a in A(s)
Initialize Q(terminal-state, .) = 0
Repeat (for each episode):
  Initialize s
  Repeat (for each step of episode):
    Choose a from s using policy derived from Q (e.g., epsilon-greedy)
    Take action a, observe r, s'
    Q(s, a) <- Q(s, a) + alpha * [r + gamma * max_a' Q(s', a') - Q(s, a)]
    s <- s'
  until s is terminal
\end{lstlisting}

\subsection{Esplorazione vs Sfruttamento}

Un pilastro fondamentale del Reinforcement Learning è il delicato compromesso tra \textbf{esplorazione} (exploration) e \textbf{sfruttamento} (exploitation):

\begin{itemize}
  \item \textbf{Esplorazione}: L'agente sacrifica il guadagno immediato per raccogliere informazioni che potrebbero portare a profitti molto più alti in futuro.
  \item \textbf{Sfruttamento}: L'agente sceglie le azioni che hanno dato buoni risultati in passato per ottenere ricompense immediate e sicure.
\end{itemize}

Per garantire la convergenza ad un risultato ottimo, è necessario esplorare tutte le coppie stato-azione con sufficiente frequenza nel lungo periodo. I principali metodi utilizzati nella pratica sono:

\begin{itemize}
  \item \textbf{\( \varepsilon \)-greedy}: 
    Scegliere l'azione in modo avido (\textit{greedy}) per la maggior parte del tempo (con probabilità \( 1 - \varepsilon \)) e selezionare un'azione in modo casuale con probabilità \( \varepsilon \).
  \item \textbf{Softmax (o Boltzmann)}: 
    tratta tutte le azioni esplorative allo stesso modo. Questo metodo assegna una probabilità di selezione a ciascuna azione ponderandola in base al suo valore atteso \( Q(s,a) \).\\
    La probabilità di scegliere l'azione \( a \) nello stato \( s \) è definita come:
    \[
      p(a) = \frac{e^{Q(s,a)/T}}{\sum_{a'} e^{Q(s,a')/T}}
    \]
    Il parametro \( T \), noto come \textit{temperatura}, agisce da modulatore cruciale per il grado di esplorazione:
    \begin{itemize}
      \item Quando T presenta un valore elevato,tutte le azioni hanno probabilità simili di essere estratte (\textbf{massima esplorazione}).
      \item Quando T presenta un valore basso, l'azione con il valore \( Q(s,a) \) più alto ha una probabilità maggiore di essere scelta (\textbf{massimo sfruttamento}).
    \end{itemize}
\end{itemize}

\subsection{Spiegazione dei parametri}
\begin{itemize}
    \item \textbf{EPISODI:} Sequenze complete di interazioni agente-ambiente, dall'inizio fino allo stato terminale (goal) o al raggiungimento del limite massimo di passi (\texttt{max\_steps}).
    \item \textbf{\(\alpha\) - LEARNING RATE:} Determina il peso delle nuove informazioni rispetto a quelle passate. Regola la velocità di aggiornamento dei valori nella Q-table.
    \item \textbf{\(\gamma\) - DISCOUNT FACTOR:} Fattore che definisce l'importanza delle ricompense future; un valore vicino a 1 orienta l'agente verso una strategia di lungo periodo.
    \item \textbf{\(\varepsilon\) - EXPLORATION RATE:} Probabilità di scegliere un'azione casuale invece di quella ottimale stimata; fondamentale per esplorare l'ambiente.
    \item \textbf{\textit{TEMP} - TEMPERATURA:} Parametro che regola il grado di esplorazione nella strategia softmax, bilanciando l'estrazione casuale di azioni con la preferenza per quelle con valori Q più alti.
\end{itemize}

\pagebreak

\section{AMBIENTE DOORKEY}

\subsection{Ambiente Doorkey}
Per valutare le performance dell’agente è stato scelto l’ambiente DoorKey, della libreria MiniGrid. Questo ambiente presenta una chiave che l’agente deve raccogliere per sbloccare la porta e successivamente deve arrivare al quadrato verde che rappresenta il goal finale. Infatti la missione di questo ambiente è riassunta come: “usa la chiave per aprire la porta e poi raggiungi il goal”. Poiché la ricompensa viene fornita esclusivamente al completamento del task finale (ricompensa sparsa), l’ambiente rappresenta un’ottima sfida per testare le capacità di esplorazione dell’algoritmo.

\noindent Il reward di questo ambiente può essere solo di due tipi:
\begin{itemize}
    \item $1 - 0.9 \cdot (\text{step\_count} / \text{max\_steps})$ se raggiunge il goal.
    \item 0 se fallisce.
\end{itemize}

L'episodio termina quando si verifica una delle seguenti condizioni:

\begin{itemize}
    \item L'agente raggiunge con successo il \textit{goal}.
    \item Viene superato il numero massimo di passi consentiti per un singolo episodio (definito dalla variabile \texttt{max\_steps}).
\end{itemize}

\subsubsection{Spazio delle Azioni}
Le azioni possibili all'interno di questo ambiente sono 7, identificate da un numero:

\begin{enumerate}
    \item \textbf{LEFT (0):} ruotare a sinistra.
    \item \textbf{RIGHT (1):} ruotare a destra.
    \item \textbf{FORWARD (2):} muoversi in avanti di una cella.
    \item \textbf{PICKUP (3):} raccogliere un oggetto (la chiave).
    \item \textbf{DROP (4):} rilasciare un oggetto.
    \item \textbf{TOGGLE (5):} interagire con un oggetto (aprire la porta).
    \item \textbf{DONE (6):} terminazione volontaria.
\end{enumerate}

\subsection{Scelta dell'algoritmo}
Per l’addestramento dell’agente è stato impiegato l’algoritmo Q-Learning. Esso permette di stimare iterativamente la funzione di valore ottimo Q(s, a) senza necessitare di una conoscenza a priori delle dinamiche di transizione dell’ambiente. Grazie all’utilizzo di una policy di esplorazione (come l’$\epsilon$-greedy e Softmax), l’agente è in grado di bilanciare la scoperta di nuove strategie con lo sfruttamento delle conoscenze già acquisite, un elemento cruciale per superare il problema delle ricompense sparse introdotto dalla meccanica della porta chiusa.

\subsection{Setup dell'Addestramento}
La fase di addestramento è stata strutturata in due diverse configurazioni sperimentali per valutare l'impatto dei parametri esplorativi e della durata massima degli episodi sulle performance di convergenza dell'agente. Entrambi i set di esperimenti sono stati condotti per un totale di 5000 episodi, mantenendo costanti il learning rate ($\alpha$) e il fattore di sconto ($\gamma$). I test sono stati suddivisi come segue:

\begin{itemize}
    \item \textbf{Configurazione 1:} Parametri impostati con $\alpha = 0.1$, $\gamma = 0.99$ ed un tasso di esplorazione iniziale $\epsilon$ oppure $TEMP = 0.9$. Il limite massimo di passi per episodio (\texttt{max\_step}) è stato mantenuto al valore di default dell'ambiente, pari a $250$.
    \item \textbf{Configurazione 2:} Parametri impostati con $\alpha = 0.1$, $\gamma = 0.99$. In questo caso, si è optato per una strategia di esplorazione iniziale più aggressiva, impostando $\epsilon$ oppure $TEMP= 1.0$. Inoltre, per concedere all'agente un orizzonte temporale più ampio per l'esplorazione casuale, il limite di passi è stato raddoppiato, portando il \texttt{max\_step} a $500$.
\end{itemize}

\subsection{Risultati Sperimentali $\epsilon$-greedy}
\label{cap:epsilon}

Nelle figure seguenti sono riportati i grafici relativi alle 10 esecuzioni (run) di addestramento. Le prime cinque misurazioni (\textbf{Figura \ref{fig:config1}}) fanno riferimento alla Configurazione 1, mentre le successive cinque (\textbf{Figura \ref{fig:config2}}) sono relative alla Configurazione 2.
\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable1.png}
        \caption{Run 1}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable2.png}
        \caption{Run 2}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable3.png}
        \caption{Run 3}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable4.png}
        \caption{Run 4}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable5.png}
        \caption{Run 5}
    \end{subfigure}
    
    \caption{Risultati ottenuti con la Configurazione 1 ($\epsilon=0.9$, \texttt{max\_step}=250).}
    \label{fig:config1}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable6.png}
        \caption{Run 6}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable7.png}
        \caption{Run 7}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable8.png}
        \caption{Run 8}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable9.png}
        \caption{Run 9}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable10.png}
        \caption{Run 10}
    \end{subfigure}

    \caption{Risultati ottenuti con la Configurazione 2 ($\epsilon=1.0$, \texttt{max\_step}=500).}
    \label{fig:config2}
\end{figure}

\clearpage

\subsection{Analisi e Confronto delle Configurazioni di $\epsilon$-greedy}

Osservando i dati raccolti, emerge chiaramente che la \textbf{Configurazione 2} riesce a ottenere risultati ottimali e, soprattutto, ampiamente riproducibili nel tempo, a differenza della Configurazione 1 che mostra una maggiore instabilità o una convergenza verso policy sub-ottimali. 

Questo netto miglioramento delle performance è attribuibile a due fattori chiave:
\begin{enumerate}
    \item \textbf{Maggiore orizzonte temporale:} L'incremento del parametro \texttt{max\_step} da 250 a 500 fornisce all'agente il tempo materiale per completare con successo l'intera sequenza di azioni richieste dall'ambiente DoorKey (trovare la chiave, aprire la porta, raggiungere l'obiettivo) durante i primi episodi, quando i movimenti sono puramente stocastici.
    \item \textbf{Esplorazione massimizzata:} L'utilizzo di un tasso di esplorazione iniziale più elevato ($\epsilon = 1.0$) assicura che l'agente esplori a fondo l'ambiente prima di iniziare ad affidarsi alla policy appresa (\textit{exploitation}). In un ambiente a ricompensa sparsa come questo, esplorare maggiormente all'inizio previene la convergenza prematura e permette all'algoritmo di propagare correttamente i valori Q a ritroso dal goal fino agli stati iniziali.
\end{enumerate}

\subsection{Risultati Sperimentali Softmax}
\label{cap:softmax}

Nelle figure seguenti sono riportati i grafici relativi alle 10 esecuzioni (run) di addestramento. Le prime cinque misurazioni (\textbf{Figura \ref{fig:config3}}) fanno riferimento alla Configurazione 2, mentre le successive cinque (\textbf{Figura \ref{fig:config4}}) sono relative alla Configurazione 1.

\pagebreak

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable11.png}
        \caption{Run 11}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable12.png}
        \caption{Run 12}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable13.png}
        \caption{Run 13}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable14.png}
        \caption{Run 14}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable15.png}
        \caption{Run 15}
    \end{subfigure}
    
    \caption{Risultati ottenuti con la Configurazione 2 ($TEMP=1.0$, \texttt{max\_step}=500).}
    \label{fig:config3}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable16.png}
        \caption{Run 16}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable17.png}
        \caption{Run 17}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable18.png}
        \caption{Run 18}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.6\textwidth}
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable19.png}
        \caption{Run 19}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}{0.6\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/Q-Learning/qtable20.png}
        \caption{Run 20}
    \end{subfigure}

    \caption{Risultati ottenuti con la Configurazione 1 ($TEMP=0.9$, \texttt{max\_step}=250).}
    \label{fig:config4}
\end{figure}

\clearpage

\subsection{Analisi e Confronto delle Configurazioni di Softmax}
Le prestazioni registrate con la politica di selezione Softmax confermano quanto osservato precedentemente per l'approccio $\epsilon$-greedy: la \textbf{Configurazione 2} si attesta come il setup più efficace in termini di efficienza e stabilità temporale. 

Le determinanti di tale successo risultano sovrapponibili a quelle esaminate in fase di analisi dei dati per la strategia $\epsilon$-greedy (si veda il Capitolo 2.5), confermando la solidità della configurazione indipendentemente dal meccanismo di selezione delle azioni adottato.

\subsection{$\epsilon$-greedy vs Softmax}
Le analisi condotte nei capitoli precedenti hanno evidenziato come la \textbf{Configurazione 2} garantisca prestazioni sistematicamente superiori rispetto alla Configurazione 1, indipendentemente dalla strategia di esplorazione adottata. 

Assodata l'efficacia di tale setup parametrico, si rende necessaria un'indagine comparativa volta a determinare quale tra i due approcci risulti più idoneo alle specificità dell'ambiente in esame. 

Dall'analisi dei risultati riportati nei Capitoli \ref{cap:epsilon} e \ref{cap:softmax}, emerge chiaramente come la politica $\epsilon$-greedy sia in grado di convergere verso il reward ottimale con una velocità superiore, richiedendo un numero di episodi sensibilmente inferiore rispetto alla controparte \textit{Softmax}.

\end{document}